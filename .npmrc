# Suppress Node deprecation warning for url.parse() from pnpm internals (DEP0169)
# pnpm 10.10.0 uses url.parse() internally via toNerfDart - harmless, will be fixed upstream
# This does not hide real CVEs - `pnpm audit` still reports vulnerabilities
node-options=--no-deprecation
