# DEPRECATED — migrasi ke dinamis ala BITS-Nota
# File ini tidak lagi dipakai. Gunakan root:
#   wrangler.template.jsonc + scripts/gen-wrangler.mjs → wrangler.jsonc
# Lihat README dan .env.example di root.
# Untuk kompatibilitas lama, copy dari wrangler.template.jsonc.
